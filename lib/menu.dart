import 'package:flutter/material.dart';

class Menu {
  final String name;
  final double price;
  final IconData icon;
  final Color color;

  Menu({this.name, this.icon, this.color, this.price});
}